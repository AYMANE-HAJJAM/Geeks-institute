--Q1:
--All rows are excluded because the subquery returns NULL, so result = 0.

--Q2:
--Rows with id ≠ 5 are counted (6 and 7), so result = 2.

--Q3:
--The subquery contains NULL, making all comparisons UNKNOWN, so result = 0.

--Q4:
--Rows with id ≠ 5 are counted (6 and 7), so result = 2.